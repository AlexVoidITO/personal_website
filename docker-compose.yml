version: '2'
services:
  app:
    build: .
    ports:
    - "8080:3000"
    environment:
      - SERVICE_MODE=${SERVICE_MODE}
      - LASTFM_API_KEY=${LASTFM_API_KEY}
      - LASTFM_USER=${LASTFM_USER}  
      - LAT=${LAT} 
      - LON=${LON} 
    restart: always

